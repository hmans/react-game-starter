import { useFrame } from "@react-three/fiber"

export const Systems = () => {
  useFrame((_, dt) => {
    // playerControlSystem()
    // movementSystem(dt)
    // ballSystem()
    // paddleSystem()
  })

  return null
}
